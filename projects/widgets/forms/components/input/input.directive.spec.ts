import { IdsInputDirective } from './input.directive';

describe('IdsInputDirective', () => {
  it('should create an instance', () => {
    const directive = new IdsInputDirective();
    expect(directive).toBeTruthy();
  });
});
