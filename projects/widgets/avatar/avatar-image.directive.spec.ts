import { IdsAvatarImageDirective } from './avatar-image.directive';

describe('IdsAvatarImageDirective', () => {
  it('should create an instance', () => {
    const directive = new IdsAvatarImageDirective();
    expect(directive).toBeTruthy();
  });
});
