import { IdsSentenceCasePipe } from './sentence-case.pipe';

describe('IdsSentenceCasePipe', () => {
  it('create an instance', () => {
    const pipe = new IdsSentenceCasePipe();
    expect(pipe).toBeTruthy();
  });
});
