import { IdsRadioGroupDirective } from './ids-radio-group.directive';

describe('IdsRadioGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new IdsRadioGroupDirective();
    expect(directive).toBeTruthy();
  });
});
