import { IdsSelectTriggerDirective } from './select-trigger.directive';

describe('IdsSelectTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new IdsSelectTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
