import { IdsChipRemoveDirective } from './chip-remove.directive';

describe('IdsChipRemoveDirective', () => {
  it('should create an instance', () => {
    const directive = new IdsChipRemoveDirective();
    expect(directive).toBeTruthy();
  });
});
