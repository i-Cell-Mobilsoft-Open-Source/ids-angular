import { ConnectedPosition } from '@angular/cdk/overlay';

export const IdsOverlayPanelConnectedPositions = {
  originX: 'start',
  originY: 'bottom',
  overlayX: 'start',
  overlayY: 'top',
} as ConnectedPosition;
